asset_index:
  mode: "infile"
  assets:
    default_fallback1: "fallback text"
    default_fallback2: "多國語言にほんごالعربيةTiếng việtไทย"
    redirect_fallback: "\"DOUBLE QUOTE\""
    not_in_used_fallback: "this is not used..."
  fallbacks:
    redirects:
      redirect_cyclic: ["test-course", "test-language"]
      redirect_double_fallback: ["", "fallback-language"]
