asset_index:
  mode: "ondisk"
  assets:
    - default_fallback: "asset1.png"
    - redirect_fallback: "asset2.txt"
  fallbacks:
    default:
      target_path: "../fallback-language/"
      keys:
        - "default_cyclic"
        - "default_double_fallback"
